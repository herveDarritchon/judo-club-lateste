/**
 * Interface to define the UserData
 * @interface UserData
 * @public
 */
export interface UserData {
	id: number;
	email: string;
	nicename: string;
	firstName: string;
	lastName: string;
	displayName: string;
}